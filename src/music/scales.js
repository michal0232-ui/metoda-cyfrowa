// New scale families can be registered here without changing pitch spelling.
// Minor exercises will also need their own cadence and resolution definitions.
export const SCALES = Object.freeze({
  major: { name: "dur", intervals: [0, 2, 4, 5, 7, 9, 11] },
});
